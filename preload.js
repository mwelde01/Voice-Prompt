// Preload script - runs in renderer before web page loads
// contextIsolation is enabled; keep this minimal
window.addEventListener('DOMContentLoaded', () => {
  // Nothing needed here - Web Speech API is available natively
});
